const slides=[...document.querySelectorAll('.slide')];let index=0;const pageInfo=document.getElementById('pageInfo');function show(i){index=Math.max(0,Math.min(slides.length-1,i));slides.forEach((s,n)=>s.classList.toggle('active',n===index));if(pageInfo) pageInfo.textContent=`${index+1} / ${slides.length}`;}document.getElementById('prevBtn')?.addEventListener('click',()=>show(index-1));document.getElementById('nextBtn')?.addEventListener('click',()=>show(index+1));document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft')show(index-1);if(e.key==='ArrowRight')show(index+1);});show(0);