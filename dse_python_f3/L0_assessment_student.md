# L0 · 入学摸底测评 · 学生版

> **时长**：90 分钟（每题平均 6 分钟）
> **说明**：这是诊断性测评，**不评分**，只帮助老师了解你的 Python 基础。
> **要求**：
> - 选择题：把答案写在题号旁边
> - 代码题：先在纸上预测输出，再敲进 [online-python.com](https://www.online-python.com/) 验证
> - 编程题：用 online-python.com 写完后，**截图发给老师**
>
> **准备好纸笔**，很多题需要"心算"Python 输出。

---

## D1 · 基础语法识别（3 题，8-25 分钟）

### 第 1 题 · print 与 input

下面哪一行是"让电脑说话"（输出一句话）？

```python
A. input("Hello")
B. print("Hello")
C. name = "Hello"
D. f"Hello {name}"
```

你的答案：____

---

### 第 2 题 · 变量名合法性

下面 5 个变量名，**哪几个合法**？（多选）

```python
A. my_age
B. 2name
C. _score
D. class
E. userName
```

你的答案：____

---

### 第 3 题 · `=` vs `==`

下面 2 行代码分别是什么意思？

```python
age = 18      # ①
age == 18     # ②
```

- A. ① 赋值；② 比较
- B. 两者一样
- C. ① 比较；② 赋值
- D. ① 输出；② 输入

你的答案：____

---

## D2 · Dry Run 输出预测（4 题，25-50 分钟）

### 第 4 题 · 变量覆盖

不看代码运行结果，**写出 print 的输出**。

```python
a = 5
a = 10
a = a + 1
print(a)
```

你的答案：____

---

### 第 5 题 · 字符串 vs 整数加法

不看代码运行结果，**写出 print 的输出**。

```python
x = "5"
y = "3"
print(x + y)
```

你的答案：____

---

### 第 6 题 · input 永远返回字符串

如果用户输入 5 和 3，下面代码输出什么？

```python
a = input("First: ")   # 用户输入 5
b = input("Second: ")  # 用户输入 3
print(a + b)
```

你的答案：____

---

### 第 7 题 · if-elif-else 输出

不看代码运行结果，**写出所有 print 的输出**。

```python
score = 75
if score >= 90:
    print("A")
elif score >= 60:
    print("Pass")
else:
    print("Fail")
print("Done")
```

你的答案：____

---

## D3 · 类型与 Bug 修复（3 题，50-68 分钟）

### 第 8 题 · input 没转 int

下面代码会怎样？**修复它**。

```python
birth = input("出生年份: ")
age = 2025 - birth
print(age)
```

会怎样：____

修复后代码：

```python
# 在这里写你的修复版本

```

---

### 第 9 题 · print 漏引号

下面代码会怎样？**修复它**。

```python
print(Hello)
```

会怎样：____

修复后代码：

```python
# 在这里写你的修复版本

```

---

### 第 10 题 · 字符串拼接 int（两种修法）

下面代码会怎样？**用至少一种方法修复**。

```python
age = 15
print("你" + age + "岁")
```

会怎样：____

修复方法 1（用 `str()`）：

```python

```

修复方法 2（用 f-string）：

```python

```

---

## D4 · IPO 算法设计（3 题，68-78 分钟）

### 第 11 题 · 自我介绍（限时 4 分钟）

**任务**：输入你的名字，输出 `Hello, [名字]!`

提示：

- 第 1 步：用 `input()` 接收名字
- 第 2 步：用 `print()` 输出

在 online-python.com 写完后，**截图发给老师**。

---

### 第 12 题 · 两数相加（限时 4 分钟）

**任务**：输入两个整数，输出它们的和。

提示：

- `input()` 永远返回字符串，想算数要先转成整数

在 online-python.com 写完后，**截图发给老师**。

---

### 第 13 题 · 简单判断（限时 4 分钟）

**任务**：输入一个分数（0-100），如果 ≥60 输出 "Pass"，否则输出 "Fail"。

提示：

- 需要 `if` ... `else`
- 别忘了 `int()` 转换

在 online-python.com 写完后，**截图发给老师**。

---

## D5 · 考试场景适应（2 题，78-90 分钟）

### 第 14 题 · 三角形分类（限时 6 分钟）

**任务**：输入三条边 `a / b / c`，按下面规则输出三角形类型：

| 类型 | 规则 |
| --- | --- |
| Invalid | 任意两边之和小于等于第三边 |
| Equilateral | a == b == c |
| Isosceles | 任意两边相等（但不是等边）|
| Scalene | 其他情况 |

**测试用例**（你可以用这些值验证）：

| 输入 (a, b, c) | 预期输出 |
| --- | --- |
| 10, 10, 10 | Equilateral |
| 10, 15, 15 | Isosceles |
| 10, 12, 15 | Scalene |
| 10, 10, 20 | Invalid |

在 online-python.com 写完后，**截图发给老师**。

---

### 第 15 题 · 求 1+2+...+n（限时 6 分钟）

**任务**：输入 n，输出 1+2+3+...+n 的和。

**测试用例**：

| 输入 n | 预期输出 |
| --- | --- |
| 5 | 15 |
| 10 | 55 |
| 100 | 5050 |

提示（如果你不会）：

- 想一个变量存累计结果（初值 0）
- 想一个循环，从 1 走到 n
- 每次循环把当前数加到累计变量

在 online-python.com 写完后，**截图发给老师**。

---

## 提交方式

完成后，把以下内容发给老师：

1. **选择题答案**（第 1-3 题、第 4-7 题）
2. **Bug 题修复后的代码**（第 8-10 题）
3. **第 11-15 题的 online-python 截图**（每题一张）

---

**测评结束，谢谢配合！**